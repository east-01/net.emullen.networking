using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace EMullen.Networking {
    /// <summary>
    /// Will appear once connected to the server, connects player data to server
    /// </summary>
    public class NetworkPlayerDataManager : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}
